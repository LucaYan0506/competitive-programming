#include<iostream>
#include<vector>
#include<algorithm>
#include<set>
#include <cstdint>

#define int long long 
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))

using namespace std;

int32_t main(){
    int t; cin >> t;
    vector<int> res;
    while(t--){
        
    }

    for (auto x : res)
        cout << x << endl;

    return 0;
}